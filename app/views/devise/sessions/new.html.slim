#devise-sessions-new-view
  devise-sessions-new-view(:errors="#{resource.errors.full_messages}" :action="'#{session_path(resource_name)}'")

= javascript_pack_tag 'devise/sessions/new'
= stylesheet_pack_tag 'devise/sessions/new'

